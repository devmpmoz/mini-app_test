<import-sjs name="element" from="/utils/element.sjs"/>

<view class="{{ element.classAttr('m-divider ' + type + (type === 'title' ? ' m-caption caption-10 secondary' : ''), className) }}" onTap="onTap">
  {{ title }}
</view>